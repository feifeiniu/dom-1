window.dom = {
  // create(tagName){
  //   return document.createElement(tagName)
  // }
  create(string ){
    const container = document.createElement("template");//template标签可以容纳任意元素
    container.innerHTML = string.trim() ;//字符串两边去空客
    return container.contend.firstChild;
  }
 
};

 